spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/examinai-db
    username: user
    password: pass
  liquibase:
    enabled: true
    driver-class-name: org.postgresql.Driver
  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
  cloud:
    config:
      enabled: false